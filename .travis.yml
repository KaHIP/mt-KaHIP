language: cpp

compiler:
  - gcc
  - clang 

before_script:
  - sudo apt-get -qq update
 
addons:
  apt:    
    packages:
     - numactl 
     - tbb
     - g++-7 
        

script: 
  - mkdir build
  - cd build 
  - cmake ../
  - make  
